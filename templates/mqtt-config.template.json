{
  "projectName": "${PROJECT_NAME}",
  "deviceId": "${DEVICE_ID}",
  "mqtt": {
    "host": "${MQTT_HOST}",
    "port": ${MQTT_PORT}
  },
  "monitoring": ${MONITORING_ENABLED},
  "security": ${SECURITY_ENABLED}
}
