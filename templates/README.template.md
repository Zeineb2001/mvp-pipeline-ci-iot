# ${PROJECT_NAME} Setup

## Description

This artifact was generated automatically by the CI pipeline.

## MQTT Configuration

- Device ID: `${DEVICE_ID}`
- MQTT Host: `${MQTT_HOST}`
- MQTT Port: `${MQTT_PORT}`
- Monitoring enabled: `${MONITORING_ENABLED}`
- Security enabled: `${SECURITY_ENABLED}`

## Run

```bash
docker compose up -d
```

## Files

- `mqtt-config.json`
- `docker-compose.yml`
- `.env`
- `README.md`
